<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - Cửa hàng</title>
    <link rel="stylesheet" href="/css/admin/addBlog.css">
    <link rel="stylesheet" href="/css/headeradmin.css">
</head>
<body>
    <%- include('../partialsadmin/headeradmin') %>




    <main>
        <div class="content">
            <div class="sales-female">
                <h2>Thêm Blog</h2>
                <form action="/admin/addBlog/add" method="POST">
                    <label>Tiêu đề:</label>
                    <input type="text" name="title" required>
                    
                    <label>Nội dung:</label>
                    <textarea name="content" required></textarea>
                    
                    <label>Chọn Sản phẩm (Không bắt buộc):</label>
                    <select name="idSP">
                        <option value="">Không chọn</option>
                        <% products.forEach(product => { %>
                            <option value="<%= product.idSP %>"><%= product.name %></option>
                        <% }) %>
                    </select>
                    
                    <label>Chọn Danh mục (Không bắt buộc):</label>
                    <select name="idDM">
                        <option value="">Không chọn</option>
                        <% categories.forEach(category => { %>
                            <option value="<%= category.idDM %>"><%= category.name %></option>
                        <% }) %>
                    </select>

                    <button type="submit">Thêm</button>
                </form>

            </div>
        </div>
    </main>
</body>
</html>