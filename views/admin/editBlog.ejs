<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog - Cửa hàng</title>
    <link rel="stylesheet" href="/css/admin/editBlog.css">
    <link rel="stylesheet" href="/css/headeradmin.css">
</head>
<body>
    <%- include('../partialsadmin/headeradmin') %>




    <main>
        <div class="content">
            <div class="sales-female">
                <h2>Sửa Blog</h2>
                <form action="/admin/editBlog/edit/<%= blog.idBlog %>" method="POST">
                    <label>Tiêu đề:</label>
                    <input type="text" name="title" value="<%= blog.title %>" required>
                    
                    <label>Nội dung:</label>
                    <textarea name="content" required><%= blog.content %></textarea>
                    
                    <label>Chọn Sản phẩm (Không bắt buộc):</label>
                    <select name="idSP">
                        <option value="">Không chọn</option>
                        <% products.forEach(product => { %>
                            <option value="<%= product.idSP %>" <%= blog.idSP == product.idSP ? "selected" : "" %>><%= product.name %></option>
                        <% }) %>
                    </select>
                    
                    <label>Chọn Danh mục (Không bắt buộc):</label>
                    <select name="idDM">
                        <option value="">Không chọn</option>
                        <% categories.forEach(category => { %>
                            <option value="<%= category.idDM %>" <%= blog.idDM == category.idDM ? "selected" : "" %>><%= category.name %></option>
                        <% }) %>
                    </select>

                    <button type="submit">Cập nhật</button>
                </form>
            </div>
        </div>
    </main>
</body>
</html>